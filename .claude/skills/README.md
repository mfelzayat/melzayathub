Skills extracted from https://github.com/anthropics/claude-plugins-official

The `/plugin` marketplace system is Claude Code CLI only, so these skills are
committed directly so Claude Code on the web loads them from `.claude/skills/`.

Skills that require CLI-only features (hooks, MCP servers, local filesystem
outside the repo) are included but will only be fully useful when running in
the terminal. Delete any directory you don't want.
